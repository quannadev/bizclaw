# Build
/target

# Editor
*.swp
*.swo
.DS_Store

# Environment & secrets
.env
.env.*
*.log
.secrets

# Local BizClaw data (contains API keys)
/.bizclaw/
.bizclaw_credentials

# Credentials and tokens
*.key
*.pem
*.crt
credentials.json

# Non-production — AI dev toolkit (local only)
.agent/
.agents/
.gemini/
plans/
.trae/

# Internal DEV files — NOT for public repo
GEMINI.md
GETTING_STARTED.md
update.sh

# Deploy & infra scripts — contain VPS IP, credentials
deploy-landing.sh
deploy*.sh
scripts/
!scripts/install.sh
!scripts/bump-version.sh

# Production infrastructure — NEVER commit to public
deploy/nginx/
docker-compose.prod.yml

# OS
Thumbs.db
mission-control/

# Sensitive deploy workflow (credentials)
_agents/workflows/deploy.md
dist/
Bizino-Dev_Kit-main/
*.apk
/dashboard/
.env.local

# Local Git tokens and credentials
.git/.git_token
.git/.gh_token

# Security sensitive
*.pem
*.key
*.crt
*.p12
*.pfx

# Audit reports
cargo-audit-report.*
trivy-results.*

# Coverage reports with potential secrets
coverage/
*.lcov
